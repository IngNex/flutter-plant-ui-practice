import 'package:flutter/material.dart';

//Colores que vamos usar para la app
const nexPrimaryColor = Color(0xFF0C9889);
const nexTextColor = Color(0xFF3C4046);
const nexBackgroundColor = Color(0xFFF9F8FD);

//Cantidades que vamos usar para la app
const double nexDefaultPadding = 20.0;
